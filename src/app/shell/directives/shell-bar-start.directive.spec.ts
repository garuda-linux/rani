import { ShellBarStartDirective } from './shell-bar-start.directive';

describe('ShellLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellBarStartDirective();
    expect(directive).toBeTruthy();
  });
});
