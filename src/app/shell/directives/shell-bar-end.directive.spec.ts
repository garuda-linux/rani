import { ShellBarEndDirective } from './shell-bar-end.directive';

describe('ShellBarEndDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellBarEndDirective();
    expect(directive).toBeTruthy();
  });
});
