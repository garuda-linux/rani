import { ShellBarLinkDirective } from './shell-bar-link.directive';

describe('ShellBarLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellBarLinkDirective();
    expect(directive).toBeTruthy();
  });
});
